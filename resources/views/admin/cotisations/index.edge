@layout.main()

  <h1>Saisons</h1>

  <ul>
  @each(saison in saisons)
    <li><a href="{{ route('admin_cotisations.show', { id: cotisation.id }) }}">{{ saison.saison }}</a>
    <form method="POST" action="{{ route('admin_cotisations.destroy', { id: cotisation.id }) }}?_method=DELETE">
     {{ csrfField() }}
  <button type="submit">Supprimer</button>
</form>
    </li>
  @endeach
  </ul>
@end
