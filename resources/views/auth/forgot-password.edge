<form method="POST" action="/forgot-password">
  {{ csrfField() }}
  <label>Email</label>
  <input type="email" name="email" required />
  <button type="submit">Envoyer le lien</button>
</form>

@if(flashMessages.info)
  <p>{{ flashMessages.info }}</p>
@endif
